As name suggests Data Definition Language defines and manage the database.
It doesn't manipulate data/records inside rows instead it modifies column. 
You can vusialize it as a box rather than toys inside the box.


1. CREATE DATABSE School;
-  It is the first step, you must create a database first then add data.


2. CREATE TABLE Students (
   Id INT PRIMARY KEY, 
   Name VARCHAR (50),
   Class INT,
   Address VARCHAR (50)
);
-  It  creates a table named as Student, with columns ID, Name, Class, Address.


3. 

